# Core dependencies with pinned versions (replace versions with your known working ones)
Abg==2.3.1
deep-translator==1.11.4
pyromod==1.0.4
openai==1.16.1
aiofiles==23.2.1
aiohttp==3.9.3
asyncio==3.4.3
beautifulsoup4==4.12.3
dnspython==2.5.0
ffmpeg-python==0.2.0  # Needs FFmpeg buildpack
gitpython==3.1.42
hachoir==3.1.4
heroku3==5.0.3
httpx==0.27.2
lyricsgenius==3.0.1
motor==3.3.2
wget==3.2
pillow==9.5.0
psutil==5.9.8
pytube==15.0.0
unidecode==1.3.7
matplotlib==3.8.3
python-dotenv==1.0.1
pyyaml==6.0.1
requests==2.31.0
speedtest-cli==2.1.3
spotipy==2.23.0
tgcrypto==1.2.3
urllib3==2.2.1

# Avoid uvloop & pykeyboard on Heroku (commented out, optional)
# uvloop==0.19.0
# pykeyboard  # Not compatible with headless servers like Heroku

# Safe GitHub packages (if absolutely necessary, pin to commit hashes)
# These increase build time and may break Heroku installs
# Prefer PyPI equivalents if possible

# pyrogram (dev branch) — make sure it's stable
git+https://github.com/KurimuzonAkuma/pyrogram@dev

# yt-dlp — try using PyPI version unless the GitHub one is necessary
git+https://github.com/yt-dlp/yt-dlp@master

# youtube-search-python
git+https://github.com/alexmercerind/youtube-search-python@main

# youtube_search (older lib)
git+https://github.com/joetats/youtube_search@master

# yt-dlp-oauth2 wrapper
git+https://github.com/TheChampu/yt-dlp-youtube-oauth2@main

# Optional / Risky packages (can break pip install on Heroku)
# py-tgcalls==1.2.9
# ntgcalls==1.1.2
